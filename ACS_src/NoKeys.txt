#library "NoKeys"
#include "zcommon.acs"

script "NoRedCard" ENTER
{
	if(CheckInventory("NoRedCard"))
	{
	TakeInventory("NoRedCard",1);
	}
	else
	Delay(1);
	restart;
}

script "NoBlueCard" ENTER
{
	if(CheckInventory("NoBlueCard"))
	{
	TakeInventory("NoBlueCard",1);
	}
	else
	Delay(1);
	restart;
}

script "NoYellowCard" ENTER
{
	if(CheckInventory("NoYellowCard"))
	{
	TakeInventory("NoYellowCard",1);
	}
	else
	Delay(1);
	restart;
}

script "NoRedSkull" ENTER
{
	if(CheckInventory("NoRedSkull"))
	{
	TakeInventory("NoRedSkull",1);
	}
	else
	Delay(1);
	restart;
}

script "NoBlueSkull" ENTER
{
	if(CheckInventory("NoBlueSkull"))
	{
	TakeInventory("NoBlueSkull",1);
	}
	else
	Delay(1);
	restart;
}

script "NoYellowSkull" ENTER
{
	if(CheckInventory("NoYellowSkull"))
	{
	TakeInventory("NoYellowSkull",1);
	}
	else
	Delay(1);
	restart;
}